import { StrsampleDirective } from './strsample.directive';

describe('StrsampleDirective', () => {
  it('should create an instance', () => {
    const directive = new StrsampleDirective();
    expect(directive).toBeTruthy();
  });
});
